# wooyerine/homebrew-tap

Homebrew tap for [Desktop Pet](https://github.com/wooyerine/desktop-pet).

```bash
brew tap wooyerine/tap
brew trust wooyerine/tap
brew install --cask desktop-pet
```

The cask is updated automatically by `scripts/update-cask.sh` in the
desktop-pet repo on every release.
